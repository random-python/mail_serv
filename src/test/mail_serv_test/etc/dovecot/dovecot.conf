
doveadm_password = abrakadabra

ssl_client_ca_file = /hello/client

doveadm_port = 1234

mail_home = /home/data/%d/%n

mail_location = maildir:~/mail:LAYOUT=fs:DIRNAME=_m_a_i_l_:UTF-8

plugin {

    sieve_dir = ~/sieve
    
    sieve = ~/active.sieve
    
    sieve_default = ~/active.sieve
    
    syncer_dir = ~/syncer
    
    syncer_pipe = /run/dovecot/syncer/pipe

}
